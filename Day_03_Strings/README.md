# Day 03 - Python Strings

## 📚 Topics Covered

- Introduction to Strings
- Creating Strings
- Single Quotes (' ')
- Double Quotes (" ")
- Triple Quotes (""" """)
- Multiline Strings
- String Length using `len()`
- Positive Indexing
- Negative Indexing
- String Slicing
- Slicing with Step
- Reverse String
- String Concatenation
- String Repetition
- Case Conversion
  - `upper()`
  - `lower()`
  - `title()`
  - `capitalize()`
- Removing Spaces
  - `strip()`
  - `lstrip()`
  - `rstrip()`
- Replacing Text using `replace()`
- Finding Text using `find()`
- Counting Characters using `count()`
- Membership Operators
  - `in`
  - `not in`
- Checking Prefix and Suffix
  - `startswith()`
  - `endswith()`
- Splitting Strings using `split()`
- Joining Strings using `join()`
- String Validation Methods
  - `isalpha()`
  - `isdigit()`
  - `isalnum()`
- Formatted Strings (f-Strings)
- String Immutability

---

## 💻 Programs Practiced

- Create and print strings
- Find string length
- Access characters using indexing
- Extract text using slicing
- Reverse a string
- Convert string case
- Remove leading and trailing spaces
- Replace words in a sentence
- Find a word position
- Count character occurrences
- Check if text exists inside another string
- Split a sentence into a list
- Join list elements into a string
- Validate string contents
- Display formatted output using f-strings

---

## 📝 Key Learning

- Strings are sequences of characters.
- Strings are immutable (cannot be changed directly).
- Indexing starts from **0**.
- Negative indexing starts from **-1**.
- Slicing extracts part of a string.
- `split()` converts a string into a list.
- `join()` converts a list into a string.
- f-Strings make formatting easy and readable.

---

## 💡 Example

```python
name = "Chandan"

print(name.upper())
print(name.lower())
print(name[::-1])
print(len(name))
```

### Output

```
CHANDAN
chandan
nadnahC
8
```

---

## 🔗 Data Science Connection

Strings are one of the most common data types in data analysis.

You will frequently use string operations to:

- Clean datasets
- Remove extra spaces
- Standardize names
- Split text into columns
- Format reports
- Validate user input
- Prepare text for Machine Learning and NLP

---

## 🎯 Skills Gained

After completing Day 03, I can:

- Create and manipulate strings
- Use indexing and slicing
- Reverse strings
- Search and replace text
- Count characters
- Format output with f-strings
- Split and join strings
- Clean textual data

---

## 📂 Files in this Folder

- `README.md` → Day 03 Notes
- `strings.py` → Practice Programs
- `output.txt` → Program Output
